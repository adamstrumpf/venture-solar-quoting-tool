# Session — 2026-07-02

## Summary
First execution session on the deployed app. Connected local folder + git, then shipped PDF export and branding/fonts.

## Work done
- **Fonts fixed** — `index.html` referenced `Outfit` / `JetBrains Mono` in `app.jsx` but never loaded them. Added Google Fonts `<link>` + a base `body` font-family and favicon.
- **Branding added** — app header now shows a `VENTURE HOME` wordmark (amber accent), the tool name, and contact info (`(888) 522-9161`, `contact@venturehome.com`).
- **PDF export wired up** — the `Generate PDF Quote` button previously had no `onClick`. Implemented `generatePDF()` in `app.jsx`: builds a branded letterhead quote (quote #, date, line items, total, electrician note, 30-day terms) and prints via a hidden iframe → browser "Save as PDF". No new dependencies.

## Tier 1 facts
- [Tier 1] Company brand is **Venture Home** (formerly Venture Solar); site `venturehome.com`; phone `(888) 522-9161`; support email `support@venturehome.com` (customer-facing quotes use support@, not contact@). Serves ME, NH, RI, MA, CT, NY, NJ, MD, PA — matches app `serviceStates`.
- [Tier 1] **Build bug fixed:** repo had no committed `package-lock.json`, but `Dockerfile` runs `npm ci` (which requires a lockfile). Generated and committed `package-lock.json`. `npm ci` + `npm run build` now succeed.

## Decisions
- PDF via browser-native print (hidden iframe) rather than a library (jsPDF etc.) — zero dependencies, keeps the Docker build simple and reproducible for a non-developer owner.
- Quote number format: `VH-YYYYMMDD-HHMM`.

## Deploy / workflow notes
- Local folder is now git-initialized and tracks `origin/main`. Owner pushes with `git push` (needs their GitHub login) and deploys with `gcloud run deploy venture-solar-quoting-tool --source . --region us-east1 --allow-unauthenticated`.
- Changes committed locally this session; **not yet pushed or deployed** (those steps are the owner's).

## Rebrand (aesthetics pass)
- [Tier 1] Brand palette: **green `#73FFC6`** + **near-black `#231F20`**. Logo is the lowercase "venturehome" wordmark (assets: horizontal + stacked, black + green).
- Switched app from dark theme to **light theme** to match venturehome.com (user chose this direction).
- Real logo added at `public/vh-logo.svg` (black horizontal wordmark), shown in app header via `<img src="/vh-logo.svg">`.
- PDF letterhead rebranded: inlined the logo SVG (constant `LOGO_SVG` in app.jsx, so print never waits on a network image), green rule, dark total pill with green figure, green electrician note.
- Contact email changed contact@ → **support@venturehome.com**; removed the "Whole-Home Solar · Battery · EV Charging" tagline from the PDF.
- Verified visually with headless Chromium screenshots (app empty/filled + PDF) — all render correctly.

## Address input + service-type changes
- Replaced the **State** dropdown with a free-form **Customer Address** field (cleaner on the customer-facing quote). State is derived in the background by `detectStateFromAddress()` — matches full state name, then 2-letter code, then ZIP prefix (9 service states). Shows "Detected state: X" under the field; warns in red if undetermined.
- Electrician logic, alert, and PDF note now use the detected state; the PDF **Location** shows the full customer address.
- Removed service type **"System Reconnection"**. Renamed **"Panel/Inverter Replacement" → "Component Replacement"** (updated the electrician-required service list too).
- Verified with headless screenshots (address → MA detected, electrician rate applied, PDF Location shows address).

## Google Places autocomplete + form polish
- Added Google Places Autocomplete to the Customer Address field. Loads Maps JS (Places) at runtime only if a key is present; on select, captures `formatted_address` + `administrative_area_level_1` (state). Graceful fallback to plain text + `detectStateFromAddress()` when no key.
- [Tier 1] **Runtime key config pattern**: `public/config.js` sets `window.APP_CONFIG.googleMapsApiKey`. In prod, `docker-entrypoint.sh` regenerates `dist/config.js` from env var `GOOGLE_MAPS_API_KEY` on container start (set via Cloud Run `--update-env-vars`, no rebuild needed). `index.html` loads `/config.js` before the app. Dockerfile now uses the entrypoint (CMD → docker-entrypoint.sh).
- Hid the visible "Detected state" text (kept a red note only for out-of-area / missing state). Removed asterisks from labels — all Quote Details fields are treated as required.
- [Tier 1] Env var name: **`GOOGLE_MAPS_API_KEY`** (documented in `.env.example`).
- Pending on user: create the Maps API key (Places API + Maps JS API, referrer-restricted), set it as the Cloud Run env var, redeploy.

## Open / next
- Pricing logic still to fix: temp-removal $190/$225/$325 by contract date (only $325 handled); material quantity/SKU logic is a rough guess (`panels/2` for wiring/other).
- Verify PDF visually in a browser once deployed.
